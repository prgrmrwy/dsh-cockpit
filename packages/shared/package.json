{
  "name": "@dsh-cockpit/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types across cockpit server and web",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test --experimental-strip-types tests/*.test.ts",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
