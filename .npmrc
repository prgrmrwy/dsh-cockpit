# dsh-cockpit 默认镜像:直连 npmjs(registry.npmjs.org),不依赖内网镜像。
#
# 背景:仓库 pnpm-lock.yaml 按 npmjs 语义生成(锁定版本以 npmjs 为准,例如
# 2026-09-02 的 tldts@7.4.11);用户级 ~/.npmrc 指向 bnpm.byted.org,该镜像
# 未同步 7.4.11 时,worktree session 的 `pnpm install --frozen-lockfile`
# 会因 tarball 404 失败(ERR_PNPM_FETCH_404)。
# 统一到 npmjs 保证「lockfile 生成」与「安装」走同一来源;需要特殊源的包
# 可在此追加 scope 级覆盖。
registry=https://registry.npmjs.org/
